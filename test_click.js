const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5500/'); await page.waitForTimeout(2000); const type = await page.evaluate(() => typeof openHomeworkModal); console.log('typeof openHomeworkModal:', type); await page.evaluate(() => openHomeworkModal()); await page.waitForTimeout(500); const display = await page.evaluate(() => { const m = document.getElementById('homeworkModal'); return m ? { dp: m.style.display, op: window.getComputedStyle(m).opacity, vz: window.getComputedStyle(m).visibility, w: m.offsetWidth, h: m.offsetHeight, z: window.getComputedStyle(m).zIndex } : 'NULL'; }); console.log('metrics:', display); await browser.close(); })();
