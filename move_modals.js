const fs = require('fs'); let html = fs.readFileSync('index.html', 'utf8'); const startStr = '        <!-- Add Homework Modal -->'; const endStr = '        <!-- AI Admin Password Modal -->'; let startIndex = html.indexOf(startStr); let endIndex = html.indexOf(endStr); if (startIndex !== -1 && endIndex !== -1) { let extracted = html.substring(startIndex, endIndex); html = html.substring(0, startIndex) + html.substring(endIndex); let targetStr = '        <script src=\"js/data.js\"></script>'; let targetIndex = html.indexOf(targetStr); if (targetIndex !== -1) { html = html.substring(0, targetIndex) + extracted + '\n' + html.substring(targetIndex); fs.writeFileSync('index.html', html, 'utf8'); console.log('Moved successfully.'); } else { console.log('Target not found'); } } else { console.log('Markers not found'); }
